sub Main()
    dim a as Set of Number
    dim b as Set of Number
    if a > b then print "no"
end sub

--output--
Compiler error
kTypeMismatch
Main
4:10
The ">" operator does not support the type "Set of Number".
