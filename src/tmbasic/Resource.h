#pragma once

#include "../common.h"

extern uint8_t kResourceHelp[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceHelp_len;

extern uint8_t kResourceRunnerLinuxArm32[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerLinuxArm32_len;

extern uint8_t kResourceRunnerLinuxArm64[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerLinuxArm64_len;

extern uint8_t kResourceRunnerLinuxX64[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerLinuxX64_len;

extern uint8_t kResourceRunnerLinuxX86[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerLinuxX86_len;

extern uint8_t kResourceRunnerMacX64[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerMacX64_len;

extern uint8_t kResourceRunnerWinX64[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerWinX64_len;

extern uint8_t kResourceRunnerWinX86[];  // NOLINT(cppcoreguidelines-avoid-c-arrays,modernize-avoid-c-arrays)
extern uint kResourceRunnerWinX86_len;
